# Dockerfile for Python 3.11 judger environment
FROM python:3.11-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
    ca-certificates \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /judge

RUN useradd -m -u 1000 judgeuser
USER judgeuser

CMD ["/bin/bash"]
